version: '1'

services:
  custom_llm:
    build: ./custom_llm/
    command: sleep infinity
    stdin_open: true
    tty: true
    ports:
      - "11434:11434"
  db:
    build: ./database
    environment:
      SA_PASSWORD: "z48%Mnt4q]"
      ACCEPT_EULA: "Y"
    ports:
      - "1433:1433"
    volumes:
      - ./database-data:/var/opt/mssql

volumes:
    database-data: